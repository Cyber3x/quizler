import { MoonIcon, SunIcon } from "@chakra-ui/icons"
import {
  Box,
  Heading,
  HStack,
  IconButton,
  Text,
  Link,
  useColorMode,
} from "@chakra-ui/react"

const Landing = () => {
  return <Text>hello from landing</Text>
}

export default Landing
